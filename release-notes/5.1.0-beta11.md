# Battery SmartFlow AI 5.1.0-beta11

## Dashboard

- Hardware topology battery-pack cards now show each pack's live state of charge as a subtle left-to-right background fill.
- The fill uses only a valid pack-specific SoC sensor, ignores hardware minimum/maximum limits, and remains neutral when no valid pack SoC is available.
- Bump the dashboard asset version to 1.0.13 and refresh its cache key.
